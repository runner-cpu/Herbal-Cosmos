# 本草宇宙网页审计与改进基线

## 目标

把项目从“包含多张图表的文化展示页”提升为“可探索、可比较、可追溯、可学习的中医药数据图谱”。可视化和交互承担知识组织，不只承担装饰。

## 当前不足

### 视觉系统

- 黛蓝、米白使用面积过大，页面容易读成单一蓝米主题，朱砂、青绿和金色只作零散点缀。
- 卡片边框、圆角和阴影高度同质，首页、学习页、图表页缺乏不同的信息密度和节奏。
- 首页主体被限制在固定宽度容器内，星云没有形成真正的沉浸式第一视觉。
- 标题、说明、筛选、图例和数据标签的层级差距不足，图表页面仍有“组件堆叠感”。
- 夜读模式只替换部分变量，表格悬停、图表文字、按钮状态等细节不完整。

### 信息架构与排版

- 十个一级导航项同时出现，桌面端拥挤，移动端则形成过长菜单。
- 首页五张统计卡与后续内容没有路径关系，数据规模被展示但不能继续探索。
- “三条文化叙事”与其他说明性内容重复占据首屏之后的黄金区域。
- 方剂卡、证候卡、药食卡和非遗卡大量采用同一种网格，页面节奏单一。
- 配伍网络的控制、画布和解释混在同一面板，没有形成探索工作区。

### 数据可视化

- 性味热力图和归经柱状图只能悬停，无法点击反查对应药材。
- 方剂网络虽然可拖拽，但缺少索引、选择后的详情检查器和来源链。
- 图表缺少“当前筛选范围、数据量、选择状态”等上下文。
- 药材表格没有属性对比工具，用户很难横向理解多味药材差异。
- 移动端依赖横向滚动表格，关键信息难扫描。

### 交互与学习闭环

- 搜索只在回车后响应，没有实时结果面板和无结果引导。
- 收藏、详情、图谱和学习舱之间缺少统一的继续探索入口。
- 学习进度只有答题总数，缺少与首页路径的清晰对应。
- 图表点击后缺少明显的视觉反馈和下一步操作。
- 交互控件的键盘焦点、禁用状态和触控尺寸不够统一。

### 内容可信度与可访问性

- 来源徽标存在，但图谱选择后的来源信息不够突出。
- 图表颜色语义未形成全站一致规则。
- 部分说明使用小字号和低对比色，夜读模式下更明显。
- 页面缺少“跳到主要内容”，键盘用户需要重复穿过导航。

### 工程与发布

- 单文件便于部署，但 CSS 和渲染逻辑持续增长，重复样式较多。
- 原先 ECharts CDN 会造成离线图表空白；现已改为仓库内 `assets/vendor/echarts.min.js`，并保留文字降级。
- 缺少自动化视觉回归，当前主要依赖语法检查和人工浏览器验证。

## 本轮改进原则

- 以墨绿、真白、矿物青、朱砂和黄铜组成多层配色，减少蓝米主导。
- 导航只保留五个高频入口，其余内容收进“更多”。
- 首页使用全宽数据星图、证据数据带和五步学习路径，删除重复叙事卡。
- 药材星图加入结果计数、移动端卡片和 2-3 味药材属性对比雷达图。
- 性味归经图支持点击反查药材，并在同页展示解释面板。
- 配伍网络改为“索引栏 + 画布 + 详情检查器”，明确选择状态和来源。
- 搜索提供实时建议；所有核心操作提供可见焦点和状态反馈。
- 保持静态单文件、公开数据来源和医学科普边界不变。

## 开源参考

- Apache ECharts：交互图表、事件联动和渐进式关系图渲染模式。
- AntV G6：知识图谱工作区、邻域聚焦和详情检查器模式。
- ChineseBERT / TCM knowledge graph 类项目：中医药实体关系和来源追溯思路。
- Observable 可视化案例：把筛选、图例和解释文本放进同一探索流程。

参考仅用于交互和信息架构，不复制第三方数据、模型或视觉资产。

## 本轮二次审计与优化记录（2026-09）

### 发现的新增不足

- Logo 仍是“草”字印章，无法形成可缩放、可识别的项目符号；导航缺少语言入口。
- 药材列表与详情只有文字属性，图像没有参与识别、记忆和内容层级，学习页容易退化成单纯问答。
- 深色模式在部分固定白色面板上会出现文字对比度下降；主题按钮状态也只有文字，没有清晰图标和可访问标签。
- 数据图表虽然可点击，但用户缺少当前数据规模、读图边界和“图表 → 详情卡”的视觉提示。
- 移动端隐藏语言按钮，无法在小屏完成完整的语言切换；桌面端的主题/语言控制也缺少统一的紧凑样式。
- 图片资源若依赖远程链接会受网络和跨域影响，静态 Pages 部署需要本地可追踪资产。

### 已完成的优化

- 用“叶片 + 星轨 + 数据节点”内联 SVG 替换文字 Logo，保留中文/英文品牌名但不把文字当作图形标志。
- 增加 `output/imagegen/herbs/ginseng.png` 本地植物图谱，并建立 `HERB_IMAGE_GROUPS` 图像映射；全部代表药材、精选卡、搜索建议、详情卡、药食同源卡和收藏卡都会加载图像。
- 增加 `zh-CN / English` 双语切换，保存到 `localStorage`，同步更新导航、搜索占位、首页主文案、路由标题、学习反馈、数据边界说明和 `html[lang]`。
- 将夜读模式升级为持久化深浅主题控制，修复深色主题下固定白色面板的对比度问题，并为主题按钮提供状态化 `aria-label`。
- 首页增加数据摘要带、鼠标 spotlight、图像化本草发现区；保留 Canvas 星云作为数据氛围层，星辰点击仍能进入知识卡。
- 药材列表、详情、药食同源、非遗与收藏模块加入图片框、懒加载、图像 alt 文本和移动端图像卡片。
- 搜索建议增加药材缩略图，并继续支持键盘上下键、Enter 跳转和无结果引导。

### 验证边界

- 代表药材已扩充至 144 味并绑定独立本地图片；45 味新增图像来自带物种提示的 AI 植物学图谱裁切，页面与来源清单明确标注为科普插画，不冒充专业鉴定照片。Openverse 同步因当前网络超时未写入不确定的开放许可来源。
- 雷达图依旧标注为代表数据集的属性对照，不代表药效强弱或临床推荐。
- React Bits 中文站与妙搭网页只借鉴 spotlight、层次化滚动、紧凑控制和探索式工作区的思路，没有复制代码、素材或页面布局。
