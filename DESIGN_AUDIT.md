# 本草宇宙网页审计与改进基线

## 目标

把项目从“包含多张图表的文化展示页”提升为“可探索、可比较、可追溯、可学习的中医药数据图谱”。可视化和交互承担知识组织，不只承担装饰。

## 当前不足

### 视觉系统

- 黛蓝、米白使用面积过大，页面容易读成单一蓝米主题，朱砂、青绿和金色只作零散点缀。
- 卡片边框、圆角和阴影高度同质，首页、学习页、图表页缺乏不同的信息密度和节奏。
- 首页主体被限制在固定宽度容器内，星云没有形成真正的沉浸式第一视觉。
- 标题、说明、筛选、图例和数据标签的层级差距不足，图表页面仍有“组件堆叠感”。
- 夜读模式只替换部分变量，表格悬停、图表文字、按钮状态等细节不完整。

### 信息架构与排版

- 十个一级导航项同时出现，桌面端拥挤，移动端则形成过长菜单。
- 首页五张统计卡与后续内容没有路径关系，数据规模被展示但不能继续探索。
- “三条文化叙事”与其他说明性内容重复占据首屏之后的黄金区域。
- 方剂卡、证候卡、药食卡和非遗卡大量采用同一种网格，页面节奏单一。
- 配伍网络的控制、画布和解释混在同一面板，没有形成探索工作区。

### 数据可视化

- 性味热力图和归经柱状图只能悬停，无法点击反查对应药材。
- 方剂网络虽然可拖拽，但缺少索引、选择后的详情检查器和来源链。
- 图表缺少“当前筛选范围、数据量、选择状态”等上下文。
- 药材表格没有属性对比工具，用户很难横向理解多味药材差异。
- 移动端依赖横向滚动表格，关键信息难扫描。

### 交互与学习闭环

- 搜索只在回车后响应，没有实时结果面板和无结果引导。
- 收藏、详情、图谱和学习舱之间缺少统一的继续探索入口。
- 学习进度只有答题总数，缺少与首页路径的清晰对应。
- 图表点击后缺少明显的视觉反馈和下一步操作。
- 交互控件的键盘焦点、禁用状态和触控尺寸不够统一。

### 内容可信度与可访问性

- 来源徽标存在，但图谱选择后的来源信息不够突出。
- 图表颜色语义未形成全站一致规则。
- 部分说明使用小字号和低对比色，夜读模式下更明显。
- 页面缺少“跳到主要内容”，键盘用户需要重复穿过导航。

### 工程与发布

- 单文件便于部署，但 CSS 和渲染逻辑持续增长，重复样式较多。
- 原先 ECharts CDN 会造成离线图表空白；现已改为仓库内 `assets/vendor/echarts.min.js`，并保留文字降级。
- 缺少自动化视觉回归，当前主要依赖语法检查和人工浏览器验证。

## 本轮改进原则

- 以墨绿、真白、矿物青、朱砂和黄铜组成多层配色，减少蓝米主导。
- 导航只保留五个高频入口，其余内容收进“更多”。
- 首页使用全宽数据星图、证据数据带和五步学习路径，删除重复叙事卡。
- 药材星图加入结果计数、移动端卡片和 2-3 味药材属性对比雷达图。
- 性味归经图支持点击反查药材，并在同页展示解释面板。
- 配伍网络改为“索引栏 + 画布 + 详情检查器”，明确选择状态和来源。
- 搜索提供实时建议；所有核心操作提供可见焦点和状态反馈。
- 保持静态单文件、公开数据来源和医学科普边界不变。

## 开源参考

- Apache ECharts：交互图表、事件联动和渐进式关系图渲染模式。
- AntV G6：知识图谱工作区、邻域聚焦和详情检查器模式。
- ChineseBERT / TCM knowledge graph 类项目：中医药实体关系和来源追溯思路。
- Observable 可视化案例：把筛选、图例和解释文本放进同一探索流程。

参考仅用于交互和信息架构，不复制第三方数据、模型或视觉资产。

## 本轮二次审计与优化记录（2026-09）

### 发现的新增不足

- Logo 仍是“草”字印章，无法形成可缩放、可识别的项目符号；导航缺少语言入口。
- 药材列表与详情只有文字属性，图像没有参与识别、记忆和内容层级，学习页容易退化成单纯问答。
- 深色模式在部分固定白色面板上会出现文字对比度下降；主题按钮状态也只有文字，没有清晰图标和可访问标签。
- 数据图表虽然可点击，但用户缺少当前数据规模、读图边界和“图表 → 详情卡”的视觉提示。
- 移动端隐藏语言按钮，无法在小屏完成完整的语言切换；桌面端的主题/语言控制也缺少统一的紧凑样式。
- 图片资源若依赖远程链接会受网络和跨域影响，静态 Pages 部署需要本地可追踪资产。

### 已完成的优化

- 用“叶片 + 星轨 + 数据节点”内联 SVG 替换文字 Logo，保留中文/英文品牌名但不把文字当作图形标志。
- 增加 `output/imagegen/herbs/ginseng.png` 本地植物图谱，并建立 `HERB_IMAGE_GROUPS` 图像映射；全部代表药材、精选卡、搜索建议、详情卡、药食同源卡和收藏卡都会加载图像。
- 增加 `zh-CN / English` 双语切换，保存到 `localStorage`，同步更新导航、搜索占位、首页主文案、路由标题、学习反馈、数据边界说明和 `html[lang]`。
- 将夜读模式升级为持久化深浅主题控制，修复深色主题下固定白色面板的对比度问题，并为主题按钮提供状态化 `aria-label`。
- 首页增加数据摘要带、鼠标 spotlight、图像化本草发现区；保留 Canvas 星云作为数据氛围层，星辰点击仍能进入知识卡。
- 药材列表、详情、药食同源、非遗与收藏模块加入图片框、懒加载、图像 alt 文本和移动端图像卡片。
- 搜索建议增加药材缩略图，并继续支持键盘上下键、Enter 跳转和无结果引导。

### 验证边界

- 代表药材已扩充至 144 味并绑定独立本地图片；45 味新增图像来自带物种提示的 AI 植物学图谱裁切，页面与来源清单明确标注为科普插画，不冒充专业鉴定照片。Openverse 同步因当前网络超时未写入不确定的开放许可来源。
- 雷达图依旧标注为代表数据集的属性对照，不代表药效强弱或临床推荐。
- React Bits 中文站与妙搭网页只借鉴 spotlight、层次化滚动、紧凑控制和探索式工作区的思路，没有复制代码、素材或页面布局。

## 信息架构与数据层二次审计（2026-09-22）

### 新发现

- “典籍时光”独立页只有一张柱状图和一条时间线，导航成本高于内容价值；与首页学习路径重复。
- 首页的 18,817 资源统计容易被理解成当前可逐条探索的数据量，和实际精品层样本之间存在口径落差。
- 收藏没有账号体系，若继续叫“我的收藏”容易让用户误以为支持云同步；但完全删除会损失学习闭环。
- 全量数据不适合直接进入 3D 星云、性味图表和详情卡，否则会污染统计口径并造成浏览器性能问题。

### 本轮处理

- 将典籍柱状图与时间线合并到首页，在 `#/classics` 保留兼容跳转并自动定位首页锚点，导航中删除独立入口。
- 在探索本草页加入“精品知识卡 / 全量本草索引”双层切换：精品层保持 144 味完整知识卡和所有属性图表；索引层由 2,283 条原始名称清洗为 2,275 条可检索名称，支持来源筛选、关键词搜索和每页 48 条分页。
- 新增 `data/herb-catalog.js` 与 `scripts/build-herb-catalog.mjs`，每条全量索引标记 `catalog-only`，并在页面明确“暂无完整四气、五味、归经、功效字段”。
- 将收藏页改名“本机收藏”，保留 `localStorage` 作为无登录的本地学习闭环，并显示数据保存边界。
- 首页摘要同时展示资源总量、精品卡数量和可检索名称数量，减少统计误读。

### 验证记录

- 本地 HTTP 服务下验证首页、`#/herbs` 两层切换、索引搜索、旧 `#/classics` 兼容跳转、`#/saved` 本机收藏、`#/qiwei` 性味图表和 `#/zheng` 病证药链。
- 通过内嵌脚本 `new Function` 语法检查、目录脚本 `node --check` 和浏览器控制台错误检查。
- 典籍合并后的首页锚点、2,275 条索引分页（48 页）、本机收藏说明均在可访问性树中可见；桌面首屏和移动窄屏卡片模式已检查。

## 首页信息架构再审计（2026-09-22）

### 对照参考站点后的主要不足

- 首页首屏结束后直接进入学习路径，缺少“数据总览 → 分类速览 → 功能入口”的连续阅读路径；新用户需要先理解导航才能开始。
- 核心统计分散在首屏证据栏与摘要栏，缺少一个明确的“当前馆藏规模”板块，2,275 条名称索引与 18,817 种资源之间的口径关系不够直观。
- 药食同源、文化与非遗、病证药链等内容各自独立，页面本身较短，首页没有承担内容预览和导流职责。
- 精选本草原来更像普通卡片列表，图片、分类和下一步动作之间的关联不足，视觉上不如参考站点的图像入口明确。
- 项目数据来源、版本规模和使用边界只在页脚出现，用户在浏览过程中难以及时建立信任。

### 本轮调整

- 首页新增深色“本草档案”数据看板：同时展示资源总量、精品知识卡、名称索引和关系网络方剂，并用口径说明区分统计层级。
- 新增功效分类速览，按精品知识卡数量排序；点击分类会带着筛选状态进入探索本草页。
- 新增六个图像化主题入口：本草星图、性味归经、配伍网络、病证药链、药食同源、文化与非遗；图片使用现有本地图谱，保持静态 Pages 可用。
- 将精选本草标题与版式改为“精选本草图鉴”，并保留图片、四气、五味、归经和详情跳转。
- 首页底部新增项目介绍、数据来源、版本规模与使用边界，独立页面仍保留深度探索和完整交互。

### 参考与验证

- 参考用户提供的“中国地方畜禽品种数字博物馆”首屏结构：核心数据看板、分类速览、功能入口、最新条目、项目说明与数据更新信息；未复制其代码、素材或布局。
- 本地浏览器验证首页新增板块均出现在可访问性树中；分类按钮可跳转并应用“补虚”等功效筛选；原有探索本草、典籍兼容路由、收藏、性味归经和病证药链继续可访问。
- 内嵌 JavaScript 语法检查与 `git diff --check` 通过；桌面首页截图确认新增看板、分类和六个主题入口均无溢出。

## 2026 implementation audit

- The approved/review catalog boundary is generated from committed source manifests and checked by validate-catalog; uncertain names remain auditable and hidden from default search.
- The homepage now previews food-and-medicine and culture/heritage content, while the standalone routes remain available.
- Global theme, herb context, saved drawer, lazy catalog loading, sparse star-map labels and insight charts are implemented as zero-build modules.
- Browser regression commands: npm run test:browser and python -m http.server 4173.
